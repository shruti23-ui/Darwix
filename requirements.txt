# Core NLP
vaderSentiment>=3.3.2

# TTS backends
elevenlabs>=1.9.0
gTTS>=2.5.0

# Audio post-processing (only needed for pyttsx3 backend, not used on Streamlit Cloud)
pydub>=0.25.1

# Web interface
streamlit>=1.35.0

# REST API (optional, for api/server.py)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
